import React from 'react'

const LaunchKey = () => { //will be used to show symbols next to launches to show tentative, success, or fail
  return (
    <div>

    </div>
  )
}

export default LaunchKey
